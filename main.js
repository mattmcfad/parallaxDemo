

$('.img-holder').imageScroll({
    coverRatio: 0.5,
    speed: 0.2,
    extraHeight: 0,
});

$('.img-holder-slow').imageScroll({
    coverRatio: 0.5,
    speed: 0.7,
    extraHeight: 100,
});

  skrollr.init({
    forceHeight: false,
    smoothScrolling: true,
    

  });


